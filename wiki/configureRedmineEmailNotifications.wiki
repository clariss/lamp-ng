Sign in as administrator in Redmine. Then:
  * go to Administration / Settings / Incoming Emails
  * Tick box Enable WS for incoming emails
  * Click on button Generate Key (the key generated will be used in the redmine alias below, replacing <generated by redmine>

Add redmine user, so an email inbox is created for redmine
{{{
adduser --no-create-home --disabled-password --disabled-login redmine
}}}
Edit email aliases:
{{{
vi /etc/aliases
}}}
Add redmine alias:
{{{
redmine: "|/usr/share/redmine/extra/mail_handler/rdm-mailhandler.rb --url http://slmstudio.dyndns.org/redmine --tracker support --project support --priority normal --allow-override tracker,priority --key <generated by redmine>"
}}}
Re-start mail server
{{{
newaliases
/etc/init.d/postfix restart
}}}